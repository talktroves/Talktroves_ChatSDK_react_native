# TalkTroves Chat SDK — Example (Expo)

## Setup

```bash
cd example
npm install
npm start
```

Then press `a` for Android or `i` for iOS.

The example uses the local package at `../packages/talktroves_chatsdk` via Metro `watchFolders`.
